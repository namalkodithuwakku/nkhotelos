"use client";

export default function AccountSettings() {
  return (
    <section className="glass-card">
      <h2>Account Settings</h2>
      <p>Settings panel will be connected in a later update.</p>
    </section>
  );
}