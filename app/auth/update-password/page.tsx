"use client";
import { KeyRound, Loader2 } from "lucide-react";
import { FormEvent, useMemo, useState } from "react";
import { createClient } from "../../lib/supabase/client";
export default function UpdatePasswordPage(){const supabase=useMemo(()=>createClient(),[]);const[p,setP]=useState(""),[c,setC]=useState(""),[busy,setBusy]=useState(false),[message,setMessage]=useState(""),[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");setMessage("");if(p.length<8){setError("Use at least 8 characters.");return;}if(p!==c){setError("Passwords do not match.");return;}setBusy(true);const{error:x}=await supabase.auth.updateUser({password:p});if(x)setError(x.message);else{setMessage("Password updated. Return to Hotel OS and sign in.");setP("");setC("");}setBusy(false);}return <main className="flex min-h-screen items-center justify-center bg-[#f7f7f5] px-4"><div className="w-full max-w-md rounded-3xl border border-black/10 bg-white p-7 shadow-sm"><div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-[#fff3e5] text-[#ef7d00]"><KeyRound className="h-6 w-6"/></div><p className="mt-6 text-sm font-bold uppercase tracking-[0.18em] text-[#ef7d00]">N K Hotel OS</p><h1 className="mt-2 text-2xl font-extrabold text-[#20252b]">Set a new password</h1><form onSubmit={submit} className="mt-7 space-y-4"><input type="password" placeholder="New password" value={p} onChange={e=>setP(e.target.value)} className="w-full rounded-xl border border-black/10 px-3.5 py-3" required/><input type="password" placeholder="Confirm password" value={c} onChange={e=>setC(e.target.value)} className="w-full rounded-xl border border-black/10 px-3.5 py-3" required/>{error?<p className="text-sm font-semibold text-red-700">{error}</p>:null}{message?<p className="text-sm font-semibold text-emerald-700">{message}</p>:null}<button type="submit" disabled={busy} className="flex w-full items-center justify-center gap-2 rounded-xl bg-[#20252b] px-4 py-3 font-extrabold text-white disabled:opacity-60">{busy?<Loader2 className="h-5 w-5 animate-spin"/>:null}Update password</button></form></div></main>}
